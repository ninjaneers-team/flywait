apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flywait.fullname" . }}-test-connection"
  labels:
{{ include "flywait.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flywait.fullname" . }}']
  restartPolicy: Never
